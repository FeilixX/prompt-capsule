node_modules/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
dist/
.next/

# SQLite data (DB_PATH)
data/
*.db
*.db-shm
*.db-wal

# Env
.env
.env.*
!.env.example
!.env.test

# Test / cache
coverage/
.turbo/
.cache/
*.log

# OS
.DS_Store
Thumbs.db

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Working scratch (design mocks, renders, screenshots, image-slice source)
renders/
/visual-system-*.html

# Local AI assistant config
.claude/

# internal planning docs (specs/plans/reviews) — local only, not public
docs/superpowers/

# local RED-upload bundle — not tracked (contains ICP filing number; must never hit the public repo)
skills/xhs/
skills/*.zip
skills/xhs.zip

# codex session state (tooling, not product)
.context/
